<div class="container top-image">
  <div class="row">
    <h4>とっておきをシェアしよう</h4>
    <div class="main-image">
      <img src="https://image.freepik.com/free-vector/characters-people-their-social-network-illustration_53876-58967.jpg"
        width="400" height="300">
    </div>
    <p>Recommenはあなたのとっておきをシェアするサイトです。</p>
    <p>ジャンルは問いません、自分の好きな物をオススメしましょう。</p>

    <% if user_signed_in? %>
      <a href="/notes/new" 
        class="waves-effect waves-light btn-large btn">投稿する</a>
    <% else %>
      <a class="waves-effect waves-light btn-large btn">はじめる</a>
    <% end %>
  </div>
</div>

<%# 人気のレコメン %>
<div class="homepage">
  <div class="container">
    <p class="ninki_recommen">人気のレコメン</p>
    <% @all_ranks.first(4).each do |note| %>
    <a href="/notes/<%= note.id %>">
      <div class="index_card" style="display: flex">
        <div class="index_card_content">
          <p class="card_title"><%= note.title %></p>
          <p><%= note.content.truncate(37) %></p>

          <div class="favs_category" style="display: flex;">
            <div class="card_favs" style="display: flex;">
              <i class="samll material-icons">thumb_up</i>
              <p><%= note.favorites.count %></p>
            </div>
            <div class="catego">
              <% note.tag_list.each do |tag| %>
              <div class="category_btn">
                <%= link_to tag, notes_path(tag_name: tag) %>
              </div>
              <% end %>
            </div>
          </div>
        </div>
        <%= image_tag(note.image_url.url) %>
      </div>
    </a>
    <% end %>
  </div>
</div>



<div class="container">
  <%# 新着順 %>
    <p class="new-text">新しい投稿</p>

    <% @notes.first(4).each do |note| %>
      <div class="row">
        <div class="new-card">
          <div class="card" style="display: flex">
            <div class="icon-image">
                <li class="user_image"><%= image_tag(note.user.image.url, size: "40") %></li>
              <p><i class="samll material-icons">thumb_up</i><%= note.favorites.count %></p>
            </div>
            <div class="card-content">
              <a href="/notes/<%= note.id %>"><span class="card-title"><%= note.title %></span></a>
              <a href="/notes/<%= note.id %>">
                <p class=''><%= note.content.truncate(40)  %></p>
              </a>
            <div class="catego">
              <% note.tag_list.each do |tag| %>
              <div class="category_btn">
                <%= link_to tag, notes_path(tag_name: tag) %>
              </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    <% end %>  
</div>